spec:
  inputs:
    tenant_slug:
      description: "Per-product Postgres role/slug (PROD_TENANT_SLUG)."
    tenant_stage:
      default: "tenant"
      description: "Pipeline stage for the tenant job (the consumer must declare it)."
---
include:
  - local: /lock.yml

prod-tenant:
  stage: $[[ inputs.tenant_stage ]]
  needs: []
  image:
    name: $STACK_OPS_IMAGE
  tags:
    - self-hosted
  environment:
    name: production
  resource_group: production
  variables:
    GIT_STRATEGY: none
    PROD_TENANT_SLUG: "$[[ inputs.tenant_slug ]]"
  before_script:
    - gem install specific_install --no-document
    - gem specific_install "$STACK_AUTOMATION_REPO" -b "$STACK_AUTOMATION_SHA"
    - export PATH="$(ruby -e 'print Gem.user_dir')/bin:$PATH"
    - eval "$(ssh-agent -s)"
    - install -d -m 700 ~/.ssh
    - install -m 600 "$PROD_SSH_PRIVATE_KEY" ~/.ssh/prod_deploy_key
    - ssh-add ~/.ssh/prod_deploy_key
    - printf 'Host *\n  StrictHostKeyChecking accept-new\n' > ~/.ssh/config
  script:
    - eiseron prod tenant
  rules:
    - if: $PREVIEW_MR_IID
      when: never
    - if: $CI_COMMIT_BRANCH == "production" && $CI_PIPELINE_SOURCE != "schedule"
      when: manual
